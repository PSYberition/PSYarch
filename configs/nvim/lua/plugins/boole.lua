-- ─────────────────────────────────────────────────────────────────────────────────────────────
local R = "nat-418/boole.nvim"
local M = "boole"
-- ─────────────────────────────────────────────────────────────────────────────────────────────
local D = { -- depens
} -- ───────────────────────────────────────────────────────────────────────────────────────────
local O = { -- options
} -- ───────────────────────────────────────────────────────────────────────────────────────────
local C = { -- config
  mappings = {
    increment = "<up>",
    decrement = "<down>",
  },
  allow_caps_additions = {
    { "true", "false", },
    { "up", "down", "top", "bottom", "left", "right", },
  },
} -- ───────────────────────────────────────────────────────────────────────────────────────────
return { -- commands
  R, dependencies = D, opts = O, config = function()require(M).setup(C)
end } -- ───────────────────────────────────────────────────────────────────────────────────────
